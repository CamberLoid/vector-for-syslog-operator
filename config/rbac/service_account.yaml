apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: vector-for-syslog-operator
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager
  namespace: system
# For pulling images from private registries like ghcr.io
# Create the secret with: kubectl create secret docker-registry ghcr-cred --docker-server=ghcr.io --docker-username=<user> --docker-password=<token> -n vector-for-syslog-operator-system
imagePullSecrets:
# - name: ghcr-cred
