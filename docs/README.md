# Vector-syslog-operator

## Background

* 存在大量不可控 TCP/UDP Syslog 日志输入，并且后续还有增量

## Target

* 在 Kubernetes 环境内
* 使用 Nodeport 或 Loadbalancer 实现任意端口接收
* 每个

## CRDs

* `VectorSocketSource`
* `VectorSyslogSource`
* `Vector 

## 其他

* domain: vector-syslog-operator.camber.moe
* 
  